using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetXRLocation : MonoBehaviour
{
    private void Awake()
    {
        transform.position = new Vector3(-1.381f, 0.5f, 0.174f);
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
